# ABOUTME: Service for the hedgehog-party demo application.
# ABOUTME: ClusterIP service exposing port 80 targeting container port 8080.
---
apiVersion: v1
kind: Service
metadata:
  name: hedgehog-party
  namespace: apps
  labels:
    app: hedgehog-party
spec:
  type: ClusterIP
  selector:
    app: hedgehog-party
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
