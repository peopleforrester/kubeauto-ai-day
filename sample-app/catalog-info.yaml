# ABOUTME: Backstage catalog entry co-located with the sample app source.
# ABOUTME: Enables Backstage to discover and display this component.
apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: sample-app
  description: Sample Python Flask application with OTel instrumentation
  annotations:
    argocd/app-name: sample-app
    backstage.io/kubernetes-label-selector: "app=sample-app"
    backstage.io/kubernetes-namespace: apps
  tags:
    - python
    - flask
    - otel
spec:
  type: service
  lifecycle: production
  owner: platform-team
  system: kubeauto-idp
